int main()
{
	vector<int> vec = { 1, 1, 2, 3, 4, 5, 5, 6 };
	list<int> lst;

	unique_copy(vec.begin(), vec.end(), back_inserter(lst));

	for (auto i : lst)
		cout << i << " ";
	cout << endl;

	return 0;
}
