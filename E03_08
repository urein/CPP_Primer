#include<iostream>
#include<string>
using namespace std;

int main()
{
	string str = "Hello World";
	
	for (auto it = str.begin(); it != str.end(); ++it)
		*it = 'X';
	
	//auto it = str.begin();
	//while (it != str.end())
	//{
	//	*it = 'X';
	//	++it;
	//}
	
	cout << str << endl;
	
	return 0;
}
